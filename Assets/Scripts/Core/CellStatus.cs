namespace Core
{
    public enum CellStatus
    {
        /// <summary> Cell starting state </summary>
        Unknown,
        Revealed,
        Flag,
        QuestionMark,
        FlagWrong,
        Exploded
    }
}
